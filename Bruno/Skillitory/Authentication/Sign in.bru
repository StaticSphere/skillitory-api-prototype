meta {
  name: Sign in
  type: http
  seq: 1
}

post {
  url: {{protocol}}://{{apibase}}:{{port}}/v1/auth/sign-in
  body: json
  auth: none
}

params:query {
  ~useCookie: true
}

body:json {
  {
    "email": "{{adminEmail}}",
    "password": "{{adminPassword}}"
  }
}
